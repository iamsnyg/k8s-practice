kind: Pod
apiVersion: v1
metadata:
  name: nginx-pod
  namespace: nginx
  labels:
    app: nginx
spec:
  containers:
  - name: nginx
    image: nginx:latest
    ports:
    - containerPort: 80

# commands
# kubectl apply -f pod.yml
# kubectl get pods -n nginx
# kubectl describe pod nginx-pod -n nginx